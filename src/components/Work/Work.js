import React from 'react';
import './Work.scss';
import { Typography } from '@material-ui/core';

const Work = () => {
  return (
    <section id="work">
      <Typography variant="h3" component="h3">
        I believe a good design should tell a great story.
      </Typography>
    </section>
  );
};

export default Work;
